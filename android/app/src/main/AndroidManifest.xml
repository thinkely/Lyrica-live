<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.LyricaLive">

        <!-- Main launcher activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.LyricaLive">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Full-screen lyrics view -->
        <activity
            android:name=".ui.FullLyricsActivity"
            android:exported="false"
            android:theme="@style/Theme.LyricaLive" />

        <!-- Debug log viewer (testing only) -->
        <activity
            android:name=".ui.DebugLogActivity"
            android:exported="false"
            android:theme="@style/Theme.LyricaLive" />

        <!-- Foreground service — owns lyrics notification and media sync -->
        <service
            android:name=".service.LyricaForegroundService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- NotificationListenerService — required for cross-app MediaSession access -->
        <service
            android:name=".mediasession.LyricaMediaSessionListenerService"
            android:exported="true"
            android:label="@string/notification_service_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>

</manifest>
